<div class="homepage">
    {{#if user}}
    {{!-- User Dashboard --}}
    <div class="container py-4">
        {{!-- Financial Summary Card --}}
        <div class="card mb-4 border-0 shadow-sm">
            <div class="card-body position-relative">
                {{!-- Nút thêm giao dịch mới --}}
                <a href="/transactions/create" class="btn btn-success rounded-circle position-absolute top-0 end-0 m-3 add-transaction-btn" 
                   data-bs-toggle="tooltip" data-bs-placement="left" title="Thêm Giao Dịch Mới">
                    <i class="fas fa-plus"></i>
                </a>

                <div class="row">
                    <div class="col-md-8">
                        {{!-- Header với thông tin user --}}
                        <div class="d-flex align-items-center mb-3">
                            <div class="balance-icon me-3">
                                {{#if user.avatar}}
                                    <img src="{{user.avatar}}" alt="Avatar" class="rounded-circle" style="width: 40px; height: 40px;">
                                {{else}}
                                    <i class="fas fa-wallet fa-2x text-primary"></i>
                                {{/if}}
                            </div>
                            <div>
                                <h5 class="mb-0">Xin chào, {{#if user.username}}{{user.username}}{{else}}!{{/if}}</h5>
                                <p class="text-muted mb-0">Đây là tổng quan tài chính của bạn</p>
                            </div>
                        </div>

                        {{!-- Smart Financial Advice --}}
                        <div class="financial-advice mt-3 p-3 rounded
                            {{#if (and (eq monthlyIncome 0) (eq monthlyExpense 0))}}
                                bg-info-subtle
                            {{else if (gt balance 0)}}
                                {{#if (eq monthlyExpense 0)}}
                                    bg-info-subtle
                                {{else if (gt monthlyIncome monthlyExpense)}}
                                    bg-success-subtle
                                {{else}}
                                    bg-warning-subtle
                                {{/if}}
                            {{else if (eq balance 0)}}
                                bg-info-subtle
                            {{else}}
                                bg-danger-subtle
                            {{/if}}
                        ">
                            <div class="d-flex">
                                <div class="advice-icon me-3">
                                    {{#if (and (eq monthlyIncome 0) (eq monthlyExpense 0))}}
                                        <i class="fas fa-info-circle fa-2x text-info"></i>
                                    {{else if (gt balance 0)}}
                                        {{#if (eq monthlyExpense 0)}}
                                            <i class="fas fa-info-circle fa-2x text-info"></i>
                                        {{else if (gt monthlyIncome monthlyExpense)}}
                                            <i class="fas fa-check-circle fa-2x text-success"></i>
                                        {{else}}
                                            <i class="fas fa-exclamation-triangle fa-2x text-warning"></i>
                                        {{/if}}
                                    {{else if (eq balance 0)}}
                                        <i class="fas fa-info-circle fa-2x text-info"></i>
                                    {{else}}
                                        <i class="fas fa-exclamation-circle fa-2x text-danger"></i>
                                    {{/if}}
                                </div>
                                <div>
                                    <h5 class="mb-1">
                                        {{#if (and (eq monthlyIncome 0) (eq monthlyExpense 0))}}
                                            Hãy Bắt Đầu Quản Lý Tài Chính!
                                        {{else if (gt balance 0)}}
                                            {{#if (eq monthlyExpense 0)}}
                                                Bạn chưa có khoản chi tiêu nào!
                                            {{else if (gt monthlyIncome monthlyExpense)}}
                                                Tình hình tài chính tốt!
                                            {{else}}
                                                Cần chú ý chi tiêu!
                                            {{/if}}
                                        {{else if (eq balance 0)}}
                                            Tình Hình Tài Chính Cân Bằng!
                                        {{else}}
                                            Cảnh báo chi tiêu âm!
                                        {{/if}}
                                    </h5>
                                    <p class="mb-0">
                                        {{#if (and (eq monthlyIncome 0) (eq monthlyExpense 0))}}
                                            Hãy thêm các khoản thu chi của bạn để bắt đầu quản lý tài chính hiệu quả!
                                        {{else if (gt balance 0)}}
                                            {{#if (eq monthlyExpense 0)}}
                                                Bạn đã có thu nhập nhưng chưa ghi nhận khoản chi tiêu nào. Hãy ghi chép đầy đủ để quản lý tài chính tốt hơn!
                                            {{else if (gt monthlyIncome monthlyExpense)}}
                                                Tháng này bạn đang chi tiêu ít hơn thu nhập. Tiếp tục duy trì thói quen tốt này!
                                            {{else}}
                                                Tháng này chi tiêu của bạn đang cao hơn thu nhập. Hãy cân nhắc giảm bớt các khoản chi không cần thiết.
                                            {{/if}}
                                        {{else if (eq balance 0)}}
                                            Hãy bắt đầu ghi chép các khoản thu chi của bạn. Điều này sẽ giúp bạn quản lý tài chính hiệu quả hơn!
                                        {{else}}
                                            Chi tiêu của bạn đang cao hơn thu nhập. Hãy cân nhắc giảm bớt các khoản chi không cần thiết.
                                        {{/if}}
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="income-expense-comparison p-3 rounded bg-light h-100">
                            <h5 class="mb-3">Thu chi tháng này</h5>
                            
                            <div class="comparison-item mb-3">
                                <div class="d-flex justify-content-between mb-1">
                                    <span>Thu nhập</span>
                                    <span class="text-success">{{formatCurrency monthlyIncome}} đ</span>
                                </div>
                                <div class="progress" style="height: 8px;">
                                    <div class="progress-bar bg-success" style="width: 100%"></div>
                                </div>
                            </div>
                            
                            <div class="comparison-item mb-3">
                                <div class="d-flex justify-content-between mb-1">
                                    <span>Chi tiêu</span>
                                    <span class="text-danger">{{formatCurrency monthlyExpense}} đ</span>
                                </div>
                                <div class="progress" style="height: 8px;">
                                    <div class="progress-bar bg-danger" 
                                         style="width: {{calculatePercentage monthlyExpense monthlyIncome}}%">
                                    </div>
                                </div>
                            </div>
                            
                            <div class="comparison-result mt-4">
                                <div class="d-flex justify-content-between">
                                    <span>Chênh lệch</span>
                                    <span class="{{#if (gt (subtract monthlyIncome monthlyExpense) 0)}}text-success{{else}}text-danger{{/if}}">
                                        {{formatCurrency (subtract monthlyIncome monthlyExpense)}}
                                    </span>
                                </div>
                                <div class="text-center mt-3">
                                    <a href="/statistics" class="btn btn-sm btn-outline-primary">Xem thống kê chi tiết</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {{!-- Recent Transactions --}}
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h5 class="card-title mb-0">Giao dịch gần đây</h5>
                    <a href="/transactions" class="btn btn-link">Xem tất cả</a>
                </div>
                
                <div class="recent-transactions">
                    {{#if recentTransactions.length}}
                        {{#each recentTransactions}}
                            <div class="transaction-item p-3 mb-2 rounded {{#if (eq this.type 'expense')}}bg-danger-subtle{{else}}bg-success-subtle{{/if}}">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div class="d-flex align-items-center">
                                        <div class="transaction-icon me-3">
                                            <i class="fas {{getCategoryIcon this.categoryId.name}} fa-fw"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0">{{this.categoryId.name}}</h6>
                                            <small class="text-muted">{{formatDate this.date}}</small>
                                        </div>
                                    </div>
                                    <div class="text-{{#if (eq this.type 'expense')}}danger{{else}}success{{/if}}">
                                        {{#if (eq this.type 'expense')}}-{{/if}}{{formatCurrency this.amount}} đ
                                    </div>
                                </div>
                            </div>
                        {{/each}}
                    {{else}}
                        <div class="text-center py-4">
                            <div class="mb-3">
                                <i class="fas fa-receipt fa-3x text-muted"></i>
                            </div>
                            <p class="text-muted">Chưa có giao dịch nào</p>
                            <a href="/transactions/create" class="btn btn-primary">Thêm giao dịch đầu tiên</a>
                        </div>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
    {{else}}
    {{!-- Landing Page for Non-logged Users --}}
    <div class="hero-section text-center py-5 bg-primary text-white">
        <div class="container">
            <h1 class="display-4 fw-bold mb-4">Quản Lý Chi Tiêu Thông Minh</h1>
            <p class="lead mb-4">Kiểm soát tài chính của bạn một cách hiệu quả và dễ dàng</p>
            <div class="d-flex justify-content-center gap-3">
                <a href="/auth/register" class="btn btn-light btn-lg">Bắt đầu ngay</a>
                <a href="/auth/login" class="btn btn-outline-light btn-lg">Đăng nhập</a>
            </div>
        </div>
    </div>

    {{!-- Features Section --}}
    <div class="features py-5">
        <div class="container">
            <h2 class="text-center mb-5">Tính năng nổi bật</h2>
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="feature-card text-center p-4 h-100 border rounded shadow-sm">
                        <i class="fas fa-chart-line fa-3x mb-3 text-primary"></i>
                        <h4>Theo dõi chi tiêu</h4>
                        <p>Ghi chép và phân loại mọi khoản chi tiêu của bạn một cách dễ dàng</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card text-center p-4 h-100 border rounded shadow-sm">
                        <i class="fas fa-chart-pie fa-3x mb-3 text-primary"></i>
                        <h4>Phân tích chi tiết</h4>
                        <p>Xem báo cáo và thống kê chi tiết về tình hình tài chính của bạn</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="feature-card text-center p-4 h-100 border rounded shadow-sm">
                        <i class="fas fa-bullseye fa-3x mb-3 text-primary"></i>
                        <h4>Đặt mục tiêu</h4>
                        <p>Thiết lập và theo dõi các mục tiêu tài chính của bạn</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{/if}}
</div>

{{#if user}}
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Khởi tạo tất cả tooltips
    var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
    var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
        return new bootstrap.Tooltip(tooltipTriggerEl);
    });
});
</script>

<style>
.add-transaction-btn {
    width: 45px;
    height: 45px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    z-index: 1000;
}

.add-transaction-btn:hover {
    transform: scale(1.1);
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}

.financial-advice {
    transition: all 0.3s ease;
}

.transaction-item {
    transition: all 0.3s ease;
}

.transaction-item:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}

.recent-transactions {
    max-height: 400px;
    overflow-y: auto;
}

.recent-transactions::-webkit-scrollbar {
    width: 6px;
}

.recent-transactions::-webkit-scrollbar-track {
    background: #f1f1f1;
}

.recent-transactions::-webkit-scrollbar-thumb {
    background: #888;
    border-radius: 3px;
}

.recent-transactions::-webkit-scrollbar-thumb:hover {
    background: #555;
}

/* Đảm bảo tooltip hiển thị đúng */
.tooltip {
    font-size: 0.875rem;
}
</style>
{{/if}}